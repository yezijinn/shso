# shso 任务看板 (TASKS.md)

> 当前版本：9.0.2/283  
> 状态规范：`[ ]` 待办 | `[/]` 执行中 | `[x]` 完成 | `[!]` 阻塞/需人工确认

---

## 🎯 当前主目标
支持对 `/data` 目录下的文件/子目录进行 Linux 权限（chmod）、所有者（chown owner）及用户组（chown :group）的修改。严格遵守零圆角、原生 M3 控件、路径注入过滤及安全校验规范。

---

## 📋 任务流水线

### 1. 底层命令封装与安全校验 (RootFileManager)
- [x] 在 `RootFileManager.kt` 封装 `changePermissions`、`changeOwner` 与 `changeGroup`
  - 范围限制：仅允许路径前缀为 `/data`（或 `/data/`）的路径执行，且必须通过既有的 `isUnsafePath` 校验过滤 `..`、`\`、`\0` 及单引号转义。
  - 参数白名单校验：
    - 八进制权限模式：严格正则匹配 `^[0-7]{3,4}$`。
    - 所有者（Owner）：仅允许匹配 `^[a-zA-Z0-9._-]+$` 或纯数字 UID。
    - 用户组（Group）：仅允许匹配 `^[a-zA-Z0-9._-]+$` 或纯数字 GID。
     - 执行命令：构造 `chmod <mode> '<escaped_path>'` 与 `chown <owner>:<group> '<escaped_path>'` 并通过 root 执行。
     - 测试要求：新增单元测试，覆盖合法与非法权限掩码、非法用户名/用户组注入、非 `/data` 路径越权拦截矩阵。
  - 产物：`RootFileManager.kt` 新增 `/data` 路径边界、八进制权限模式及 owner/group 白名单校验，并通过 `RootService.escapeShellArg` 执行 `chmod`、`chown owner` 与 `chown :group`；新增 `RootFileManagerEscapingTest.kt` 安全矩阵测试。
  - 验证：`./gradlew.bat :app:testDebugUnitTest` 通过（BUILD SUCCESSFUL，26 actionable tasks）；Kotlin LSP 未配置。

### 2. 直角权限与所有权编辑弹窗 (FilePermissionDialog)
- [x] 编写纯 Compose 原生直角弹窗组件 `FilePermissionDialog`
  - UI 铁律：弹窗、内部输入框、按钮等所有组件形状严格为 `RoundedCornerShape(0.dp)`，禁止引入外部 UI 库，禁止使用 `RectangleShape` 或 `CircleShape`。
  - 色彩令牌：使用 `AuroraTokens` 颜色与极光暗色样式，禁止书写装饰性 `Color(0x...)` 字面量。
   - 交互功能：
    - 权限部分：3x3 读/写/执行直角复选框（User/Group/Others），与下方 3~4 位八进制数值双向联动。
    - 所有权部分：提供独立直角 TextField 分别编辑 Owner 与 Group；预设快捷直角填入按钮（如 `root:root`、`system:system`）。
     - 确认提交：输入内容校验通过后，通过挂起函数派发至 IO 线程执行。
   - 产物：新增 `ui/components/FilePermissionDialog.kt`，包含九宫格权限位与八进制双向转换、Owner/Group 编辑、`root:root`/`system:system` 预设、格式校验及挂起提交回调；新增 `FilePermissionDialogTest.kt` 覆盖权限转换与非法模式矩阵。组件统一使用 `AuroraWindowDialog`、`AuroraTokens` 与 `RoundedCornerShape(0.dp)`。
   - 验证：`./gradlew.bat :app:testDebugUnitTest` 通过，76 tests completed；Kotlin LSP 未配置，组件尚未接入 FilePage，未执行设备端渲染验证。

### 3. FilePage 入口集成与数据链路打通
- [x] 在 `FilePage.kt` 接入属性编辑入口
  - 触发条件：在文件列表项长按或更多菜单中新增“权限/属性”操作项（当且仅当当前所处路径在 `/data` 下时激活或展示）。
   - 状态同步：点击后读取目标文件当前的实际权限与所有者，回填展示至弹窗；操作成功后自动触发目录就地无感刷新，失败时使用既有提示机制给出报错。
   - 布局约束：严格保持页面内对底部 DockBar 的 `padding(bottom = 56.dp)` 预留，禁止擅自增加 `navigationBarsPadding()`。
   - 产物：`RootFileManager.kt` 新增 `/data` 限定的权限元数据读取与权限字符串转八进制解析；`FilePage.kt` 在动作菜单接入“权限/属性”，保存成功后刷新并复用 `feedbackMessage`；`FilePermissionDialog.kt` 增加成功回调以保持 Compose 状态在主线程更新。
   - 验证：`./gradlew.bat :app:testDebugUnitTest` 与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；Debug APK 已安装至 `BIYLBAFQQSS8DA69`，前台启动 `com.mixradio.droid/.MainActivity`。Kotlin LSP 不可用，设备截图工具超时，未声称弹窗视觉验收通过。

### 4. 自动化构建与规范验证
- [x] 运行完整单元测试与构建校验
  - 验证命令 1：`./gradlew :app:testDebugUnitTest`
  - 验证命令 2：`python3 build_apk.py`（验证原生 M3 规范与 APK 打包签名）
   - 验证命令 3：`./gradlew :app:assembleDebug`
   - 验证结果：`./gradlew.bat :app:testDebugUnitTest`、`python build_apk.py` 与 `./gradlew.bat :app:assembleDebug` 均通过；Release APK 为 `app/build/outputs/apk/release/app-release.apk`（29.80 MB），原生 Material 3 校验及 V2/V3 签名校验通过。

---

## ⚠️ 阻塞与缺陷记录 (BLOCKED LOG)
- 暂无阻塞

### 自主维护：权限解析回归覆盖
- [x] 补充特殊权限位与异常输入测试
  - 检查项：锁定 `stat` 权限字符串解析对 setuid/setgid/sticky 标记、长度错误和非法字符的处理契约。
  - `RootFileManager.kt`: `permissionStringToOctal` 现保留 setuid/setgid/sticky 高位，并校验权限串首字符类型；非法长度、文件类型或权限位返回 `null`。
  - `RootFileManagerEscapingTest.kt`: 新增 `4755`/`2755`/`1755`/`7777` 特殊权限及异常输入回归测试。
  - 验证：`./gradlew.bat :app:testDebugUnitTest` 通过；Kotlin LSP 未配置。

### 自主维护：特殊权限语义修复
- [x] 修正 `S/T` 权限标记的执行位语义
  - 检查项：`s/S/t/T` 中大写标记表示特殊位存在但对应执行位关闭，解析结果不得错误增加执行权限。
  - `RootFileManager.kt`: 仅小写 `s/t` 计入执行位，大写 `S/T` 只保留 setuid/setgid/sticky 高位。
  - `RootFileManagerEscapingTest.kt`: 新增 `S/T` 无执行位断言，覆盖 `4644`、`2644`、`1644`。
   - 验证：`./gradlew.bat :app:testDebugUnitTest` 通过；Kotlin LSP 未配置。

### 自主维护：权限弹窗四位模式一致性
- [x] 保持特殊权限模式在读取与编辑之间不丢失
  - 检查项：核对 `FilePermissionDialog` 的八进制转换是否支持并保留 setuid/setgid/sticky 高位。
  - `FilePermissionDialog.kt`: `permissionBitsToOctal` 支持可选四位特殊权限前缀，权限矩阵更新时保留原有前缀并校验其范围。
  - `FilePermissionDialogTest.kt`: 新增 `4755`/`2644` 双向保持测试及非法特殊前缀测试。
   - 验证：`./gradlew.bat :app:testDebugUnitTest` 通过；Kotlin LSP 未配置。

### 自主维护：权限弹窗完整回归
- [x] 验证特殊权限模式改动不破坏整体工程
  - 检查项：运行 Debug 单元测试与 Debug APK 编译，确认权限弹窗四位模式改动可编译且既有测试保持通过。
   - 验证：`./gradlew.bat :app:testDebugUnitTest` 与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；单元测试报告 79 tests completed，Debug APK 编译成功。

### 当前需求：APK 安装过程反馈
- [x] 为 APK/XAPK 安装增加持续可见的状态反馈
  - 检查项：安装开始、进行中、成功与失败均有明显提示，安装期间避免用户重复触发或误以为应用无响应。
  - `app/src/main/java/com/mixradio/droid/ui/pages/FilePage.kt`: 新增页面级安装状态条；安装中显示文件名与“请勿重复操作”，完成/失败保留结果提示；保留 Toast 作为补充，并对安装异常生成失败结果，安装动作继续在进行中禁用。
  - 验证：`./gradlew.bat :app:testDebugUnitTest` 与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；Kotlin LSP 未配置/不可用。

### 自主维护：安装反馈布局回归
- [x] 复核安装状态条与 FilePage 底部布局约束
  - 检查项：状态条不引入额外系统 inset，不破坏列表底部 `56.dp` DockBar 预留，安装动作在进行中保持禁用。
  - 审计：安装状态条位于 `Scaffold` 内容的路径行与文件列表之间；`FilePage.kt` 未新增 `navigationBarsPadding()`，列表仍保留 `padding(bottom = 56.dp)`，系统 inset 仍由 `Scaffold.innerPadding` 与 `DockBar` 处理；安装项继续使用 `enabled = !isInstalling`。
   - 验证：`./gradlew.bat :app:testDebugUnitTest` 与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；布局扫描仅在 `DockBar.kt` 保留实际 `navigationBarsPadding()`。

### 自主维护：安装结果短时提示
- [x] 将安装完成/失败结果改为 3 秒自动消失的 alert
  - 检查项：安装中继续保持持续提示；完成/失败提示不永久占用 FilePage 列表区域，3 秒后自动清除。
  - `FilePage.kt`: 拆分安装中状态条与安装结果提示；完成/失败结果使用独立 alert 状态并通过 `LaunchedEffect` 延迟 3 秒清除，保留 Toast 和安装期间禁用重复操作。
  - 验证：`./gradlew.bat :app:testDebugUnitTest` 与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；Kotlin LSP 未配置。设备交互未代操作，由用户人工验证。

### 当前需求：文本编辑器批量文本处理
- [x] 在“设置”中增加删除空行、整体缩进两格、删除所有换行
  - 检查项：三个操作位于“字号”之前，直接更新编辑内容并标记未保存，不改变查找/对比/历史/保存/设置菜单结构。
  - `TextEditorDialog.kt`: 在字号上一行新增三个纯文本设置操作；删除空行会移除仅含空格/制表符的行，整体缩进给每行增加两个空格，删除所有换行会移除 LF/CRLF/CR；操作结果更新编辑器内容并标记未保存。
  - 产物：新增 `TextEditorTextTransformTest.kt` 覆盖三种文本转换。
  - 验证：`./gradlew.bat :app:testDebugUnitTest`（84 tests）与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；Kotlin LSP 未配置。设备操作未代替用户执行。

### 自主维护：文本处理后关闭设置
- [x] 三个批量文本处理按钮点击后自动关闭“设置”窗口
  - 检查项：删除空行、整体缩进两格、删除所有换行均先更新编辑内容，再关闭设置弹窗；其他设置项行为不变。
  - `TextEditorDialog.kt`: 三个批量文本处理按钮执行文本更新后调用现有 `onDismiss()`，自动关闭“设置”窗口；未保存状态继续由编辑器状态管理。
  - 验证：`./gradlew.bat :app:testDebugUnitTest` 与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；设备操作未代替用户执行。
