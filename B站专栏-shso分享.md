# shso —— 安卓 ROOT 脚本执行工具

shso 是一个运行在 Android ROOT 环境下的图形化执行工具：运行 `.sh` 脚本与 `.so` / ELF 原生程序，带 ANSI 高亮终端与 stdin 交互，并集成全盘 ROOT 文件管理、文本编辑与对比、压缩包解压、APK 提取与安装。界面为等宽字体 + 直角矩形深色主题，冷启动直接进入主页四 Tab（主页 / 终端 / 文件 / 设置）。

> 包名不是固定的：默认 `com.mixradio.droid` 可以换成你自己的，也能用 GitHub Actions 在网页上直接编译 APK，不必本地装 Android Studio / SDK。做法见文末「自定义包名与在线编译」。

![主页](图片链接)

## 谁适合用

- 系统：Android 8.0（API 26）及以上
- 架构：仅 arm64-v8a，32 位 ARM 与 x86 设备无法安装
- ROOT：Magisk / KernelSU / APatch（执行 ROOT 脚本、访问受保护路径需要）
- 权限：需要「所有文件访问权限」
- 无 ROOT 也能用：浏览文件、编辑文本、解压、安装普通 APK、使用书签；只有受保护路径与静默安装需要 ROOT

## 功能全览（六大模块，共 88 项）

### 一、文本编辑器

- 打开即编辑（Sora Editor 内核）
- 4MB / 5 万行文本约 0.8 秒打开
- 大文件稀疏行索引 + 虚拟滚动
- 超大文件（>32MB）只读浏览
- 外置语法包 62 语言 / 187 扩展名
- 语法按需加载 7–47ms
- 无扩展名按名匹配（Dockerfile / Makefile）
- 顶栏显示具体语言（如 .rs 显示 RS / Rust）
- 语法包管理：导入 / 停用 / 删除 / 批量启停
- 原子保存：保留原文件权限与属主
- 编码严格：不可表示字符拒绝保存
- 行尾识别并还原 LF / CRLF / CR
- 编码自动检测（BOM→UTF-8→GB18030→ISO-8859-1）与手动切换
- 编辑历史：来源标记 + 相对时间
- 编辑历史去重与原地升级
- 草稿快照守卫（只写编辑历史，不写原文件）
- 查找 / 替换（独立游标）
- 文本处理二次确认（先展示行数影响面）
- 写入 BOM 可显式开关
- 行号 / 字号可调
- 打开即编辑（无只读 / 编辑切换）
- >200KB 编辑前风险预警
- >20 万字符收起行号 / 停字数统计
- >32MB 文件退回稀疏只读浏览（全文入内存会 OOM）

### 二、文件管理

- 目录浏览 / 导航 / 上级返回
- 记忆上次浏览目录
- 快捷入口（/ · storage · adb/shso）
- 文件页搜索（名称子串 / 命中数）
- 提取 APK（命名「应用名-版本号」）
- 分包自动追加 -splitN.APK
- 分包整体安装（install 会话流）
- 兼容其他工具 split_config 聚合
- 提取后媒体库刷新
- 权限 / 账户选择器 200+（选应用回填 uid）
- 书签预置 /data/adb 与 /data/adb/modules
- 全选文件 / 取消全选
- 单文件动作菜单（编辑 / 重命名 / 拷贝…）
- 重命名 / 拷贝 / 移动 / 删除
- 权限 / 属性八进制矩阵
- 图片浏览（多格式 / 缩放 / 旋转）
- 字体预览并应用为全局等宽
- 新建文件（时间戳预填）
- 悬浮导航（回顶 / 到底 / 刷新）
- 后台长任务前台服务哨兵
- 无扩展名识别 / 语言标识
- 文件列表设置弹窗重排

### 三、终端

- ANSI 转义解析：16 色 / 256 色 / 真彩色 / 加粗，文字颜色可自定义
- 控制序列消化：`\r` 原地覆盖、`\b` 退格、`ESC[K` 行内擦除（进度条只占一行）、光标显隐等私有模式序列、OSC 窗口标题与超链接，均不残留乱码
- 状态指示（待命 / 运行中）
- 顶栏：复制 / 结束 / 重启 / 设置
- 输入行：中断 / 清屏 / 历史 / Enter（空行）/ 发送
- 键入的长命令同样有状态：顶栏显示「运行中」、输出边跑边显示、可中断 / 结束进程（按进程组整组回收）
- 运行超过 5 秒的命令自动进前台保活，切后台不被回收，通知内可直接结束
- 单行渲染上限 4000 字符：超长单行（无换行的二进制 / 压缩 JSON 输出）只显示前 4000 字符并标注，**复制输出仍是全文**
- 命令历史（最多 50 条 / 内存不落盘）
- 终端设置（颜色 / 字号 / 提示）
- 仅停在底部时自动跟随日志；发命令 / 清屏后自动回到尾部
- 日志上限 25 万字符滑动窗口
- 旋转屏幕保留输入内容、命令历史与待确认的高危命令
- Toast 统一 LONG（3.5s）
- 重启终端按协程代际清理
- 守卫就绪探测不阻塞 UI

### 四、ROOT 安全

- 运行时守卫 shso_guard
- 审计日志记录破坏性写
- 安全档位 0–3 打通
- 档位 0 / 1 红色风险提示
- 拦截编码 / 混淆脚本
- 拦截格机原语（刷机 / 格式化）
- 重定向目标检测（块设备 / 分区）
- 变量伪装命令名识别
- 脚本来源命中硬拦（手动可确认）
- 超长 / 加密一律拒绝自动执行
- 危险命令前缀绕过防护
- 分包安装防护
- Zip Slip 防护
- 解压目标不可写时禁用入口

### 五、工程与性能

- 安装包体积 2.09MB
- R8 keep 规则收窄
- 编译期零警告
- 消除 lint Error / 告警
- 依赖升级（navigation3 / core-ktx 等）
- 命名统一 47 处
- 注释规范化（去叙事、补约束）
- 文档规范 / 命名规范
- 历史遗留清理约 540 行
- 编辑历史每文件独立存储

### 六、其他能力

- 文本对比（逐行 / 重复）
- 压缩包解压 12 种格式
- 防 Zip Slip 二次校验
- 图片浏览与字体预览
- 添加到 shso（独立存储 / 自动删除 / 自动执行）
- 执行引擎（RootService / 前台服务）
- 安全模型（见下节）

## 安全模型（三层）

- 静态审查：词法切分、展开 `$(...)` / 反引号 / `sh -c` / `eval`，剥离前缀、提取重定向目标
- 运行时守卫 shso_guard：随 APK 分发，PATH 前置拦截破坏性命令，审计日志写入 `/data/adb/shso/audit.log`
- 审计日志：破坏性写操作留痕
- 安全档位 0–3：关 / 审计 / 标准（默认）/ 最高；档位 ≥2 安装运行时守卫；执行前弹风险确认框（文件名、路径、类型、大小、修改时间、SHA-256、是否 Root 执行）

## 自定义包名与在线编译

> 包名可以改成你自己的，编译也不必依赖本地环境。

- 默认包名 `com.mixradio.droid` 能换成你自己的
- 用 GitHub Actions 在线构建 APK，无需本地安装 Android Studio / SDK
- 填包名即可下载产物；完整步骤见仓库 `docs/在线编译.md`

## 获取与构建

- GitHub：github.com/yezijinn/shso（Release 下载 APK，或按文档自行编译）
- 本地构建：`./gradlew :app:assembleRelease`，或 Windows 用 `python build_apk.py`（签名 + 版本规则 + 产物校验）
- 许可证：Apache License 2.0

## 实机截图

> 上传图片后，把下面的「图片链接」替换为 B站图床地址即可（本地相对路径不会在网页端渲染）。

![文件管理](图片链接) ![终端](图片链接) ![设置·守卫](图片链接)

![书签](图片链接) ![文件列表设置](图片链接) ![文本编辑设置](图片链接)

![文本对比](图片链接) ![APK 信息](图片链接)

---

一个 2.09MB 的小工具，把 ROOT 脚本执行、文件管理、文本编辑和安全防护都装下了。常需要在手机上跑脚本、改配置、管文件的人，用着会挺顺手；包名能换成自己的，编译也能直接在网页上完成，没什么上手门槛。
