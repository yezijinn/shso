<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 「提取 APK」需要枚举全部已安装应用：targetSdk 30+ 的包可见性过滤会让
         getInstalledPackages() 只返回可见包，必须声明 QUERY_ALL_PACKAGES（本应用经 GitHub 分发，
         不走 Google Play，故无该权限的商店合规限制）。 -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Android 13（API 33）起发布通知需运行时授权；API 32 及以下系统忽略该声明。
         未授权时前台服务通知不会在通知抽屉展示，仅出现在任务管理器，故在设置页引导用户授予。 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="com.mixradio.droid.ShsoApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.shso"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="35">

        <activity
            android:name="com.mixradio.droid.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.shso">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 命令执行的前台保活服务：切后台后维持进程前台优先级，长脚本/下载/编译可持续运行 -->
        <service
            android:name="com.mixradio.droid.data.ExecutionForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 非 ROOT 安装 APK 用：向系统安装器以内容 URI 共享 APK 文件 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>
