# shso 任务看板 (TASKS.md)

> 当前版本：9.0.2/283  
> 状态规范：`[ ]` 待办 | `[/]` 执行中 | `[x]` 完成 | `[!]` 阻塞/需人工确认

---

## 🎯 当前迭代主目标
专项代码审计与缺陷排查：覆盖 ROOT 提权命令注入漏洞、前台服务生命周期泄漏、Compose 零圆角与 DockBar 布局规约违规、以及单例状态同步风险。

---

## 📋 任务流水线

### 阶段 1：ROOT 执行层与安全性审计 (RootService & RootFileManager)
- [x] 审计所有 `su -c` 调用处的路径转义与特殊字符过滤
  - 检查项：确保所有输入严格经过 `.replace("'", "'\\''")` 转义，且过滤了 `..`、`\`、`\0`（路径穿越/注入防范）。
  - `RootFileManager.kt`: 新增 `isUnsafePath`/`isUnsafeFileName`，接入路径读写、移动、复制、删除及重命名入口；确认 `RootService.escapeShellArg` 的现有 shell 引号转义完整且不改动。
  - 产物：新增 `RootFileManagerEscapingTest.kt`，覆盖路径穿越、反斜杠、NUL、换行、回车及合法路径/文件名矩阵。
  - 验证：`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug` 均通过。
- [x] 检查进程生命周期与异常中断逻辑
  - 检查项：验证执行脚本/二进制被用户手动 Cancel 时，底层 Linux 进程及管道资源（stdin/stdout/stderr）是否彻底释放，排查僵尸进程隐患。
  - `RootService.kt`: 修复 `sendInterrupt` 的任务代际句柄捕获；取消/重启后强制关闭 stdin/stdout/stderr，并对旧任务 join 增加 2 秒上限；无运行任务时不再误杀残留句柄。
  - 审计结论：原执行读取循环可能因子进程继承输出管道而阻塞；现有 kill/restart 路径已补充流关闭与有界等待，保持代际隔离。
  - 验证：`./gradlew :app:testDebugUnitTest --no-daemon` 与 `./gradlew :app:assembleDebug` 均通过。
- [x] 审计后台前台服务状态同步
  - 检查项：检查 `ExecutionForegroundService` 与 `RootService` 在长任务异常终止或崩溃时，前台服务通知能否可靠自动移除与销毁。
  - `ExecutionForegroundService.kt`: 处理通知 `ACTION_KILL` 并委托 `RootService.killCurrentProcess()`；在 `onDestroy` 显式调用 `ServiceCompat.stopForeground(STOP_FOREGROUND_REMOVE)`；同步修正文档说明，保持 `dataSync` 类型与 RootService 所有权不变。
  - 验证：`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug` 均通过；Kotlin LSP 未配置。

### 阶段 2：UI 规范与布局铁律校验 (Compose / Theme / Layout)
- [x] 静态扫描全工程 Shape 注入，排查圆角违规
  - 检查项：确认全局无任何非直角残留；严禁出现 `RectangleShape` 或 `CircleShape` 导入；检查弹窗、Menu、TextField 等是否全量使用 `RoundedCornerShape(0.dp)`。
  - 审计结论：`MainActivity` 已注入 `AuroraShapes`，五个 Material 形状槽位均为 `RoundedCornerShape(0.dp)`；`app/src/main` 显式 Shape/clip/shadow/border 命中均为直角配置，未发现 `RectangleShape`、`CircleShape` 或非零圆角。
  - 验证：`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug` 均通过；本项无生产代码修改。
- [x] 页面结构与 DockBar 布局约束复核
  - 检查项：排查 FilePage 与 SettingsPage 是否误加了 `navigationBarsPadding()`；确认 FilePage 列表外层保留了 `bottom = 56.dp` 预留。
  - 审计结论：`FilePage` 与 `SettingsPage` 均使用 `Scaffold` 的 `innerPadding`，页面未重复调用 `navigationBarsPadding()`；两页均保留 `padding(bottom = 56.dp)`，共享 `DockBar` 自身高度为 `56.dp` 并负责系统导航栏 inset，未发现布局违规。
- [x] 容器嵌套与色值字面量排查
  - 检查项：排查主页/文件/设置列表中是否存在 Card/Surface 误包裹 Row 的情况（必须是无容器纯列表）；排查是否误写了未经 AuroraTokens 令牌派生的装饰性 `Color(0x...)`。
  - 审计结论：`HomePage`、`FilePage`、`SettingsPage` 的列表行均为直接 `Row`/`Column` 结构，未发现 Card/Surface 包裹；三页面未发现装饰性 `Color(0x...)`，颜色均使用 `AuroraTokens` 或其 `copy()` 派生。`Color.Transparent` 不属于装饰性色值违规；主题令牌、DockBar 渐变及编辑器/取色器功能色板不在本项页面范围内。
  - 验证：`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug` 均通过；本项无生产代码修改。

### 阶段 3：架构单例与并发竞态检测
- [x] 全局单例（`RootService` / `AppSettings`）并发安全性审查
  - 检查项：检查状态流（StateFlow/SharedFlow）在多线程并发更新时是否存在数据竞争（Race Condition）或协程作用域未取消泄露。
  - 审计结论：本项目相关 UI 状态使用 Compose `mutableStateOf`，写入路径均在主线程；`RootService` 的 `scope` 为进程生命周期单例，不存在按任务重复创建的作用域泄露。确认 `appSettings`、`activeProcess`、`processWriter`、`executionJob` 是跨 Main/IO 访问的普通引用，存在可见性竞态风险。
  - `RootService.kt`: 为上述四个共享引用增加 `@Volatile`，保证任务代际判断和进程/管道句柄不会因 stale read 误清理新任务；未改变协程控制流程。
  - 验证：`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug` 均通过；Kotlin LSP 未配置。
- [x] 文件浏览模块大目录滑动性能与内存表现
  - 检查项：检查 Root 模式遍历海量文件（如 `/system/lib64`）时的分页/缓冲机制，排查是否存在主线程阻塞（ANR）或 OOM 隐患。
  - 审计结论：加载链路 `FilePage` → `LaunchedEffect(currentDirectory)` → `refresh()` → `RootFileManager.listFiles`，`pathExists`/`listFiles` 均以 `withContext(Dispatchers.IO)` 执行 su 子进程读取与 stat 解析，主线程无阻塞 I/O；`runCommandSync` 用独立守护线程读流并带 120s `waitFor` 超时熔断，无死锁/无界挂起；渲染侧为 `LazyColumn`（`itemsIndexed` + `path_index` 稳定 key）仅组合可视项，`FileItem` getter 均为纯字段计算、无惰性 I/O。
  - 确认事实：① **无分页/缓冲**——`listFiles` 对 `find -exec stat` 全量输出做单目录整体物化（String→`List<FileItem>`→`sortedWith`），属全量内存一次性构造；② 主线程唯一 CPU 开销是目录变更后 recomposition 阶段一次性的 `applyFileViewSettings`（O(N) 过滤 + O(N log N) `name.lowercase()` 排序），受 `remember(fileList, showHidden, sortMode)` 缓存、非逐帧。
  - 风险分级：现实 Android 目录（`/system/lib64` 数百项）下，全量物化仅数 MB、主线程排序数十~数百 ms 抖动，均远低于 ANR（>5s）阈值，**无确认 ANR/OOM**。仅 10 万+ 项病态目录才会触及阈值，非手机文件系统常态。
  - 结论：**no-change（不改代码）**。不建议引入分页/惰性加载——属任务范围外的推测性设计，渲染侧已由 `LazyColumn` 惰性化；若未来出现超大目录真实痛点，最小改动应是把 `applyFileViewSettings` 的过滤+排序移出主线程（IO 计算后返回），而非引入分页。

### 阶段 4：自动化回归与验证
- [x] 运行本地单元测试
  - 验证命令：`./gradlew :app:testDebugUnitTest`
  - 验证结果：`BUILD SUCCESSFUL`。
- [x] 运行工程原生 M3 与编译完整校验
  - 验证命令：`python3 build_apk.py` 与 `./gradlew :app:assembleDebug`
  - 验证结果：Windows 命令 `python build_apk.py` 已通过；脚本自动发现 JDK 17，Material 3 预检、`assembleRelease`、APK 定位及 V2/V3 签名校验均通过，产物为 `app/build/outputs/apk/release/app-release.apk`（29.78 MB）。`./gradlew.bat :app:assembleDebug` 亦为 `BUILD SUCCESSFUL`。

---

## ⚠️ 阻塞与缺陷记录 (BLOCKED LOG)
<!-- 当排查中发现阻断性 Bug 或连续修复失败时，在此追加详细上下文 -->
- 暂无阻塞

### 自主优化：构建环境可移植性
- [x] 修复 `build_apk.py` 对失效 JDK 绝对路径的依赖
  - 检查项：在保留原生 Material 3、签名与 APK 校验流程的前提下，确保默认 JDK 路径失效时能发现当前可用 JDK。
  - `build_apk.py`: 新增 JDK 自动发现，按 JDK 17 默认路径、Windows Adoptium JDK 17 目录、环境变量及 PATH 顺序选择可用 JDK；保留原有 Gradle 环境变量与构建校验流程。
  - 验证：`python -m py_compile build_apk.py`、`python build_apk.py` 与 `./gradlew.bat :app:assembleDebug` 均通过；Release APK 产物为 `app/build/outputs/apk/release/app-release.apk`（29.78 MB），原生 Material 3 及 V2/V3 签名校验通过。

### 自主优化：签名凭据安全
- [x] 审计并收紧构建脚本中的签名凭据来源
  - 检查项：确认签名密码不以明文保存在源码中，同时保持本地构建失败原因清晰、Gradle 签名契约不被静默改变。
  - `build_apk.py`: 删除未使用的明文 `storepass`/`keypass` 字段；签名密码继续由 `app/build.gradle.kts` 从环境变量或 `local.properties` 读取，未改变签名流程。
  - 验证：`python -m py_compile build_apk.py`、`./gradlew.bat :app:testDebugUnitTest`、`python build_apk.py` 与 `./gradlew.bat :app:assembleDebug` 均通过；Release APK V2/V3 签名校验通过。

### 自主优化：构建路径可配置性
- [x] 审计并改善 `build_apk.py` 的外部路径配置
  - 检查项：确认 Android SDK 与外部 keystore 路径不会阻塞其他已配置本机环境，同时保持默认路径和签名校验行为不变。
  - `build_apk.py` / `app/build.gradle.kts`: SDK 路径支持 `ANDROID_SDK_ROOT`/`ANDROID_HOME`，keystore 路径支持 `KEYSTORE_FILE` 或 `local.properties` 的 `signing.storeFile`；未配置时保留原默认路径。
  - 验证：`python -m py_compile build_apk.py`、`./gradlew.bat :app:testDebugUnitTest` 与串行 `./gradlew.bat :app:assembleDebug` 均通过；构建使用现有默认 SDK/keystore 配置成功。并行 assemble 的 Windows `ChildProcess.kill` 为封装层中断，非 Gradle 错误。

### 自主优化：凭据文件忽略规则
- [x] 审计并补强本地环境凭据文件的 Git 忽略规则
  - 检查项：覆盖常见 `.env` 与本地凭据变体，同时不影响已存在的源码、构建产物和显式配置文件。
  - `.gitignore`: 新增 `.env`/`.env.*` 忽略规则并保留 `!.env.example` 示例例外；既有 `local.properties`、JKS、APK 与 `.tmp` 规则保持不变。
  - 验证：`git check-ignore` 确认 `.env`、`.env.local`、`local.properties`、JKS、APK 与 `.tmp` 均被忽略；`!.env.example` 例外生效；`./gradlew.bat :app:testDebugUnitTest` 通过。

### 自主维护：构建验证记录一致性
- [x] 校正 `TASKS.md` 中过时的 JDK 构建记录
  - 检查项：使阶段 4 的记录准确反映当前 `build_apk.py` 已具备 JDK 自动发现能力。
  - 结果：删除“脚本预检受阻”“未修改脚本”“临时覆盖 `JAVA_HOME`”等过时描述，保留实际通过的构建、产物与签名校验结果。

### 自主维护：构建配置文档
- [x] 补充 `build_apk.py` 的外部环境变量说明
  - 检查项：文档准确说明 JDK、Android SDK 与 keystore 的可配置入口，且不泄露任何凭据。
  - `build_apk.py`: 在模块使用说明中补充 `JAVA_HOME`、`ANDROID_SDK_ROOT`、`ANDROID_HOME` 与 `KEYSTORE_FILE`；同步说明签名密码仅由 Gradle 从环境变量或 `local.properties` 读取。
  - 验证：`python -m py_compile build_apk.py`、`python build_apk.py --help` 与 `./gradlew.bat :app:testDebugUnitTest` 均通过。

### 当前修复：GitHub Tag 纯数字版本获取
- [x] 修复版本检查从本地 `app` 版本回退的问题
  - 检查项：更新检查成功时必须从 GitHub tag 获取纯数字版本，不得把本地 `app` 版本作为远程版本结果。
  - `SettingsPage.kt`: 严格解析 GitHub tag，仅接受 `YYYYMMDD` 或 `vYYYYMMDD` 形式的纯数字版本；HTTP 失败或无有效 tag 进入网络错误状态；`UpToDate` 保存并显示真实远程 tag，本地 `BuildConfig.VERSION_CODE` 仅用于比较。
   - 验证：`./gradlew.bat :app:testDebugUnitTest` 与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；Kotlin LSP 未配置。

### 当前修复：GitHub Actions 用户签名构建
- [x] 保证 Release Actions 构建使用用户签名密钥
  - 检查项：CI Runner 不依赖本地 Windows 路径，Release 构建使用 GitHub Secrets 临时还原的 JKS 与签名密码；缺少密钥时必须明确失败。
  - `app/build.gradle.kts`: 移除 `GITHUB_ACTIONS` 强制 debug 签名回退，仅显式 `-PuseDebugSigning=true` 才使用 debug 签名；Release 默认使用 `KEYSTORE_FILE`、`KEYSTORE_PASSWORD` 与 `KEY_ALIAS_PASSWORD`。
  - `.github/workflows/build-apk.yml`、`.github/workflows/publish-release.yml`: 新增 `KEYSTORE_BASE64` 解码到 Runner 临时目录，并将 keystore 路径与密码通过环境变量传给 Gradle；两工作流在缺少 `KEYSTORE_BASE64` 或 `KEYSTORE_PASSWORD` 时明确失败。当前两个 workflow 假定 keystore 密码与 alias 密码相同。
  - 验证：`./gradlew.bat :app:testDebugUnitTest` 与 `./gradlew.bat :app:assembleDebug` 均 `BUILD SUCCESSFUL`；`git diff --check` 通过。当前环境未安装 YAML 专用校验器/解析模块，未执行真实 GitHub Runner 验证。
  - 配置前提：仓库必须新增 Secret `KEYSTORE_BASE64`，内容为用户 JKS 的 Base64；已有 `KEYSTORE_PASSWORD` 继续使用，不要把 JKS 或密码提交到仓库。

### 当前验证：GitHub Actions 在线自定义包名构建
- [x] 使用 `com.ting.mp3.android` 触发并核验在线 Release 构建
  - 检查项：确认 workflow 能启动、Release 构建成功、用户签名密钥生效且 APK 产物可下载。
  - 验证：Run `34430059121`（[Actions](https://github.com/yezijinn/shso/actions/runs/34430059121)）成功完成包名校验、JDK/SDK 检查、JKS 恢复、Release 编译与 Artifact 上传；下载产物 `app-release.apk` 大小 31,228,709 bytes。
  - APK 核验：`aapt2 dump badging` 显示 `applicationId=com.ting.mp3.android`、`versionName=9.0.2`、`versionCode=20260910`；`apksigner verify` 确认 V2/V3 签名有效，证书主体为 `CN=Jinn`，证书 SHA-256 摘要与本地 Release APK 一致，确认使用同一用户签名密钥。
  - 历史阻塞：首次运行因缺少 `KEYSTORE_BASE64` 失败；补充 Secret 后第二次发现远程 `main` 缺少 `GuardPathPolicy.kt` 与 `ExecutionForegroundService.kt`，已补充源码并推送后重新验证通过。
