# shso 更新日志

> 格式约定：按日期倒序，同一天按「新增 / 修复 / 优化 / 安全」分组；**一条一行**，
> 只写用户可感知的变化（实现细节与证据见提交信息）。README 不再内嵌本文件内容。

## 20260912

### 新增

- 文件「权限/属性」的「所有者 / 用户组」新增账户选择器：列出系统账户与已安装应用（账户名 / uid / 应用名，200+ 项，可搜索）；选择应用时回填数字 uid —— 应用账户不在 passwd 中，`chown u0_a216` 会报 unknown user。
- 终端新增「命令历史」：动作行「历史」按钮列出本次会话执行过的命令（最多 50 条，降序），点击回填输入框，支持一键清空；仅存内存不落盘，避免命令中的密码/token 明文长期留在设备上。
- 文本编辑器改为默认**只读**进入，顶部菜单的「只读」「编辑」拆成**两个独立按钮**：当前状态用强调色 + 加粗高亮（只读选中时「只读」亮、「编辑」暗，反之亦然），一眼可分辨；只读态下替换、历史回退、文本处理等所有写入入口均被拦截并提示。
- 移除「只读（>128KB）」等历史遗留标识（默认即只读，无需重复标注），该信息条只保留分段加载进度。
- 大文件不再禁止编辑：点「编辑」会先加载全文再放开编辑与保存，卡顿属预期；读取失败或超出读取上限时拒绝进入编辑并说明原因，避免半截内容覆盖原文件。
- 保存按钮不做任何禁用：始终可点，连点幂等（保存中忽略重复请求），无改动时点击不产生副作用；有改动时以强调色提示待保存。
- 只读态不拦截保存：只读约束的是「不接收输入」，而非「不允许落盘已有改动」——先编辑、再切回只读的改动可直接点「保存」，无需再切回编辑。
- 「历史」仅在编辑态产生快照与自动保存，只读浏览不再写入历史。
- 大幅文件（>200KB）进入编辑前给出实测风险提示并提供「仍要编辑」：实测该体量文本的排版会阻塞主线程约 30 秒（数据加载本身仅 0.3 秒，瓶颈在编辑控件对超长文本的全量排版），此前会无预警卡死直至 ANR。
- 超长文本（>20 万字符）编辑时自动收起行号列并停止字数统计，首次布局耗时下降约 30%（512KB 实测：阻塞帧 2759 → 1931）。
- 编辑态超过 6.4 万字符时改用原生 `EditText` 渲染（Compose 文本控件对全文整段排版，原生走 `DynamicLayout` 增量排版）：512KB 实测主线程阻塞 **2759 帧（约 46 秒）→ 157 帧（约 2.6 秒）**，打字期间阻塞降至 1–2 帧，卡死与 ANR 消失；小文本仍走 Compose 通道以保留语法高亮。
- 超长文本的状态栏改为只补行数（8193 行）并给字节数显示占位，不再因跳过统计而显示「行数 0」。
- 修复原生编辑通道与行号列冲突：6.4 万–20 万字符区间原本同时启用原生编辑与 Compose 行号列，两套滚动体系无法同步导致行号错位，现该区间一律收起行号列。
- 修复原生通道下「查找下一个」起点错误：Compose 侧选区在回写时被置为文末，导致每次都从文末环绕查找；改用独立游标推进，替换后游标归零。
- 分段浏览模式点「查找」改为明确提示「未载入全文，点编辑后即可查找」，此前打开面板却查不到任何内容且无说明。
- **修复编辑大文件后切回只读的二次卡死**：原生编辑通道原先只在编辑态启用，切回只读会退回 Compose 通道、再触发一次整段排版（512KB 实测阻塞 2759 帧 ≈ 46 秒）。现改为按文本量启用（只看长度，不看是否可编辑），原生控件支持只读模式（禁输入、保留滚动与选词复制）；真机实测切回只读阻塞降至 148 帧 ≈ 2.5 秒。
- 修复原生通道下滚动条失效：自家滚动条绑定 Compose 滚动状态，与原生控件互不相干，拖动无效且位置错误，现原生通道下不再显示（与行号列同源问题）。
- 编辑器设置面板新增「写入 BOM」开关；同时修复三处「操作后行为不合理」：① 分段浏览态点「对比」原本会用空内容比对（无意义结果），现提示先点「编辑」；② 分段浏览态设置面板的文本处理按钮可点但无效、全文统计显示全 0（会被误读为文件为空），现文本处理禁用、统计改为说明原因并跳过无意义的全量扫描；③ 未保存对话框的「保存」按钮实际行为是保存后关闭编辑器，文案改为「保存并关闭」以免误解。：此前只能沿用读取到的 BOM 状态，无法增删；非 UTF-8/UTF-16 编码下自动禁用。
- 文本处理（删除所有空行 / 整体缩进两格 / 删除所有换行）改为二次确认：这三个操作都会整篇改写且编辑器没有撤销，现先展示「行数 X → Y」影响面再执行；若结果只剩 1 行且长度较大，额外提示超长单行排版会更慢。
- 查找面板改为 50% 不透明，并补 `FLAG_DIM_BEHIND` 暗化遮罩（该 ROM 上遮罩仍未生效，面板保持用户指定的 50%）。
- 新增 `docs/文档规范.md`（Markdown 写作准则、格式约定、文档职责边界）与 `docs/命名规范.md`（Kotlin / Compose 命名约定、项目词汇表与禁止项），README 与 AGENTS 均已挂链。

### 修复

- 全部 Toast 提示统一为 `Toast.LENGTH_LONG`（21 处，此前 19 处为 `LENGTH_SHORT` 约 2 秒）：原生 Toast 只有约 2s / 3.5s 两档，取更长的 3.5 秒以免提示过早消失。

- 修复「重启终端」状态清理条件过宽：原判断在「重启期间已有新任务启动」时会成立，把新任务误清成「待命中」，现仅按执行协程代际判定。
- 修复守卫就绪探测可能阻塞 UI 线程：缓存过期时会同步起 su 进程（最长 5s）造成 ANR，现改为立即返回上次结果并后台刷新。

### 优化

- 编辑历史增加单文件总体积上限（100 万字）、单条上限下调至 20 万字：原「20 条 × 50 万字」会把 SharedPreferences 撑到数十 MB，拖慢启动并造成内存膨胀。
- 大文件（>128KB）分段只读模式新增「只读」标识并灰显「保存」按钮，状态栏文案精简为单行，避免用户误以为可编辑。
- 安全档位为 0（无防护）或 1（仅审计）时在设置页给出红色风险提示，避免误切后长期无感知运行。
- 代码注释规范化：去除历史叙事与装饰符号，补充关键约束（DockBar 56dp 预留耦合、权限位序与特殊位、色相/明度取值区间等）。
- 命名统一：`RootService` 中同一设置的三种局部叫法合并；47 处业务语义单字母变量按作用域重命名（时间分量与画布尺寸按规范豁免）。
- 编译期零警告：7z 改用 `SevenZFile.Builder`、图片查看器改用带 centroid 的 `rememberTransformableState`、清理 63 个未使用 import。
- 消除 lint 全部 Error：`windowLightNavigationBar` 隔离到 `-v27` 资源、补全 Android 13 通知权限声明与运行时守卫、主题图标补 monochrome 层。
- 消除 lint 主要告警：`SharedPreferences.edit {}` 与 `String.toUri()` KTX 化、依赖坐标迁入 version catalog、备份规则拆分为 `dataExtractionRules` + `fullBackupContent`。
- 依赖升级：navigation3 1.1.7、core-ktx 1.19.0、lifecycle-runtime-compose 2.11.0、Compose BOM 2026.09.00、Compose 插件 1.12.0、aboutlibraries 15.2.0、hot-reload 1.2.0、benchmark 1.5.0、baselineprofile 1.5.0（Gradle 与 AGP、Kotlin 因新版依赖在当前仓库不可达，维持原版本）。
- 构建输出零警告：全量任务无编译警告、无 Gradle 弃用警告；唯一弃用由 AGP 内部产生，已抑制并在 `gradle.properties` 注明根因与复查方式。
- 合并早期设计与报告为归档（`安全设计归档.md`、`主题重构设计.md`、`artifacts/` 四份报告），去除过期表述与重复内容。

## 20260911

### 新增

- 文件页「设置」新增「提取APK」：列出已安装应用，导出安装包到内部存储 Download。
- 提取文件命名固定为「应用名-版本号.APK」（后缀大写），应用名中的非法字符自动净化。
- 分包应用提取时自动追加 `-split1.APK`、`-split2.APK` 等分片文件，确保产物可安装。
- 分包 APK 支持整体安装：点基础包或点任意分包都会聚合同一套包并整套装入。
- 分包识别兼容其他工具导出的 `split_config.*.apk`（按同包名 + 同版本号聚合）。
- 提取完成后通知媒体库刷新；正在浏览 Download 时自动出现新文件。
- 运行时守卫模块 shso_guard：拦截破坏性写操作并记录审计日志。
- 安全档位 0–3 打通：守卫自动安装、策略即时同步、高危命令确认框。
- 文件列表设置新增「全选文件 / 取消全选」，只选文件、不含文件夹。
- 文本编辑器支持行尾风格识别与保存时还原（LF / CRLF / CR）。

### 修复

- 修复分包 APK 提取出来却装不上：单文件安装报 MISSING_SPLIT。
- 修复无 ROOT 时对分包应用报错信息不明确，改为提示需要 ROOT。
- 修复终端日志出现重复行时列表直接崩溃。
- 修复大文件编辑器内容一片空白（分段内容未传给渲染列表）。
- 修复中等体积文本（0.5–2MB）打开后长时间无响应（阈值 2MB 下调为 128KB）。
- 修复「中断 / 结束进程」只杀直接子进程，导致孤儿进程持续占用 CPU。
- 修复 Root 目录下文件修改时间恒显示 1970（秒被当成毫秒）。
- 修复普通脚本被误判为「二进制 / 加密」（文件类型检测参数不被支持）。
- 修复编辑器切换编码会静默丢弃未保存的修改。
- 修复文件读取失败后仍可保存，会把原文件截断为 0 字节。
- 修复 Root 保存会改掉原文件权限，并把符号链接替换成普通文件。
- 修复 Root 目录下文件名含 `->` 时被错误截断。
- 修复编码探测整读文件，导致超大文件内存溢出崩溃。
- 修复快速切换目录时，旧目录的加载结果覆盖新目录（可能操作错路径）。
- 修复进入目录后列表空白、必须手动刷新才出现。
- 修复终端首帧在主线程全量解析日志造成明显卡帧。
- 修复主页底部内容延伸到透明导航栏下方。
- 修复解压目标目录不可写时入口仍可点击且无任何提示。
- 修复解压路径校验不足（Zip Slip 风险）。
- 修复 Root 权限探测超时无效并遗留后台进程。
- 修复危险命令可用 `timeout` / `sudo` / `stdbuf` / `env` 等前缀绕过检测。

### 优化

- release 包体积由 5.00MB 降至 4.11MB（清理未引用字体与无用图片）。
- release 包体积由 4.11MB 降至 1.84MB：移除 zstd-jni（4 份 ABI 原生库约 1.9MB）、只打包 arm64-v8a、启动图前景转 WebP、排除 commons-codec 语音词典、proguard 改为按需保留 commons-compress。
- 压缩包格式由 14 种调整为 12 种，不再支持 `.zst` / `.tar.zst`。
- 语法高亮改为停顿后在后台计算，连续输入不再掉帧。
- 大文件分段读取按完整行对齐，不再切断行与多字节字符。
- 载入文本统一换行符，旧 Mac 格式文本不再显示成一行。
- 编辑历史改为每文件独立存储，不再每次读写整份数据。
- 切换标签页保留页面状态，终端输入与文件页选择不再丢失。
- 文件列表项标识去掉下标，删除与排序不再整段重建。
- 安装包分片先复制到临时目录再生效，规避系统权限限制。
- 守卫安装改为原子替换，失败时保留或回滚旧版本。
- 审计日志改用线程安全的时间格式，并在写入前清除符号链接。
- 版本名固定为 `Jinn`，版本号采用构建当日日期。

### 安全

- 拦截编码 / 混淆脚本：`base64 | sh`、`eval` 接解码器、解释器内联解码载荷、超长 base64 单行。
- 拦截格机原语：分区表工具、刷机工具、格式化、截断设备文件、写 `sysrq-trigger`。
- 新增重定向目标检测，覆盖不经 `dd` 的块设备与系统分区写入。
- 新增变量伪装命令名与 `eval` / `xargs` 载荷识别。
- 脚本来源命中即硬拦，终端手动输入可确认执行，避免误伤正常使用。
- 脚本无法完整扫描（超长行、超大文件、疑似加密）时一律拒绝自动执行。
